/**
 * Contexto de Autenticación para Desarrolladores
 * Archivo separado para evitar warning de Fast Refresh
 */

import { createContext } from 'react';

export const DeveloperAuthContext = createContext(null);
